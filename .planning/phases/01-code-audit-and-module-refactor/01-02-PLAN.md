---
phase: 01-code-audit-and-module-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/vuln/mod.rs
  - src/vuln/redhat_enrich.rs
  - src/vuln/osv.rs
  - src/vuln/nvd.rs
  - src/vuln/distro.rs
  - src/redhat.rs
  - src/container/scan.rs
  - src/container/mod.rs
  - src/vulndb.rs
  - src/archive.rs
  - src/iso.rs
  - src/main.rs
autonomous: true
requirements:
  - QUAL-01
  - QUAL-06
must_haves:
  truths:
    - "Every .rs file under src/vuln/ is under 800 lines"
    - "Every .rs file under src/ is under 800 lines (except main.rs if Claude determines splitting it is not beneficial)"
    - "cargo build --release compiles with zero warnings"
    - "cargo test passes all 46 tests — no regressions from reorganization"
    - "All public API function signatures remain identical — callers outside the module are unaffected"
    - "The diff is reviewable — only code movement and re-exports, no functional changes"
  artifacts:
    - path: "src/vuln/redhat_enrich/"
      provides: "Split redhat_enrich module — multiple submodules each under 800 lines"
    - path: "src/vuln/osv/"
      provides: "Split OSV module — batch query, enrichment, mapping in separate files"
    - path: "src/vuln/nvd/"
      provides: "Split NVD module (if over 800 lines post-audit) — CPE matching, enrichment, keyword search"
    - path: "src/redhat/"
      provides: "Split redhat OVAL module — parsing, evaluation, caching in separate files"
    - path: "src/container/scan/"
      provides: "Split container scan — orchestrate, inventory, enrich"
    - path: "src/vulndb/"
      provides: "Split vulndb — build, query, fetch"
    - path: "src/archive/"
      provides: "Split archive — extraction logic by format or concern"
    - path: "src/vuln/mod.rs"
      provides: "Updated re-exports pointing to new submodule paths"
    - path: "src/container/mod.rs"
      provides: "Updated re-exports pointing to new submodule paths"
  key_links:
    - from: "src/vuln/mod.rs"
      to: "src/vuln/redhat_enrich/, src/vuln/osv/, src/vuln/nvd/"
      via: "pub use re-exports"
      pattern: "pub use (redhat_enrich|osv|nvd)::"
    - from: "src/container/mod.rs"
      to: "src/container/scan/"
      via: "pub use re-exports"
      pattern: "pub use scan::"
    - from: "src/main.rs"
      to: "all modules"
      via: "mod and use declarations"
      pattern: "mod (vuln|container|redhat|archive|vulndb)"
---

<objective>
Break all oversized modules (>800 lines) into focused submodules, each with a single clearly named responsibility and under 800 lines.

Purpose: Make every subsequent phase's bug fixes target the right file — no more hunting through 1,800-line files to find the right function. Per user decision, one atomic commit per module split so any individual split can be reverted independently.

Output: All source files under 800 lines (with possible exception of main.rs per Claude's discretion), clean re-exports, zero warnings, 46 tests passing.
</objective>

<execution_context>
@/Users/devintripp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devintripp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-audit-and-module-refactor/01-CONTEXT.md
@.planning/phases/01-code-audit-and-module-refactor/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split highest-priority vuln/ modules (redhat_enrich, osv, nvd) and redhat.rs</name>
  <files>
    src/vuln/redhat_enrich.rs, src/vuln/osv.rs, src/vuln/nvd.rs,
    src/vuln/distro.rs, src/vuln/mod.rs, src/redhat.rs, src/main.rs
  </files>
  <action>
    Split the highest-priority modules first — these are on the critical path for Phase 2 and Phase 3. Work in priority order. For each file, create a new directory with the same name, move the original file's content into focused submodules, and create a mod.rs that re-exports all public items to maintain the same external API.

    **CRITICAL RULE: No functional changes.** This is pure code movement. Every `pub fn`, `pub struct`, `pub enum`, and `pub type` must remain accessible at the same import path via re-exports. The ONLY changes to code are: moving blocks between files, adding `pub(crate)` or `pub(super)` visibility where needed, and updating `use`/`mod` declarations.

    **1. src/vuln/redhat_enrich.rs (1,858 lines post-audit) → src/vuln/redhat_enrich/ directory**

    Read the file and identify natural split points. Expected submodules (Claude determines exact boundaries):
    - `mod.rs` — re-exports, shared types/constants
    - A submodule for the main `redhat_inject_unfixed_cves()` orchestration function
    - A submodule for per-package CVE list fetching and parsing (the Red Hat API client code)
    - A submodule for CVE-to-finding conversion and deduplication logic
    - A submodule for the `redhat_enrich_findings()` and `redhat_enrich_cve_findings()` helper functions

    **2. src/vuln/osv.rs (993 lines post-audit) → src/vuln/osv/ directory**

    Expected submodules:
    - `mod.rs` — re-exports
    - A submodule for `osv_batch_query()` and related batch query logic
    - A submodule for `osv_enrich_findings()` and the OSV-to-finding mapping
    - A submodule for `map_osv_results_to_findings()` and helper functions

    **3. src/redhat.rs (1,351 lines post-audit) → src/redhat/ directory**

    Per CONTEXT.md, Claude assesses whether to merge with redhat_enrich or keep separate with cleaner splits. The goal is single clear responsibility per module. Expected submodules:
    - `mod.rs` — re-exports
    - A submodule for OVAL XML parsing (`parse_oval_definitions`, `parse_oval_tests`)
    - A submodule for OVAL evaluation (`evaluate_oval_for_packages`, `compare_evr`)
    - A submodule for caching (`CachedOvalData`, serialization/deserialization)

    **4. src/vuln/nvd.rs (868 lines post-audit)**

    If still over 800 lines after Plan 01 dead code removal, split into:
    - `mod.rs` — re-exports
    - A submodule for `nvd_findings_by_product_version()` and CPE configuration parsing
    - A submodule for `nvd_cpe_findings()` and `nvd_keyword_findings()`

    If under 800 lines after audit, skip — per user decision, borderline files are Claude's discretion.

    **5. src/vuln/distro.rs (838 lines)**

    If over 800 lines, split. If at or near borderline after audit, Claude's discretion. If splitting: separate the Debian/Ubuntu/Alpine feed logic into per-distro files.

    **After each split:**
    1. Update `src/vuln/mod.rs` re-exports so `pub use redhat_enrich::*`, `pub use osv::*`, etc. still work
    2. Update `src/main.rs` imports if needed (but usually vuln/mod.rs handles this)
    3. Run `cargo build --release` — must compile with zero warnings
    4. Run `cargo test --locked --no-fail-fast` — all 46 tests must pass
    5. Make ONE atomic commit: "refactor(vuln): split redhat_enrich.rs into focused submodules"
  </action>
  <verify>
    <automated>cd /Users/devintripp/Desktop/GitHub/Rust/rust_scanner && cargo build --release 2>&1 | grep -c "^warning:" | xargs -I{} test {} -eq 0 && cargo test --locked --no-fail-fast 2>&1 | grep "test result:" | grep "46 passed" && find src/vuln -name "*.rs" -exec wc -l {} + | awk '$1 > 800 && $2 != "total" {exit 1}'</automated>
  </verify>
  <done>
    - src/vuln/redhat_enrich/ exists as a directory with multiple submodules, each under 800 lines
    - src/vuln/osv/ exists as a directory with multiple submodules, each under 800 lines
    - src/redhat/ exists as a directory with multiple submodules, each under 800 lines
    - src/vuln/nvd/ split if it was over 800 lines post-audit, or left as-is with documented rationale
    - src/vuln/distro.rs split or kept with documented rationale
    - All public APIs accessible at the same import paths via re-exports in mod.rs files
    - Zero compiler warnings
    - All 46 tests pass
    - One atomic commit per module split
  </done>
</task>

<task type="auto">
  <name>Task 2: Split remaining oversized modules (container/scan, vulndb, archive, iso)</name>
  <files>
    src/container/scan.rs, src/container/mod.rs,
    src/vulndb.rs, src/archive.rs, src/iso.rs, src/main.rs
  </files>
  <action>
    Split the remaining oversized files. These are lower priority (Phase 4 dependencies) but must be done to satisfy the "all files under 800 lines" success criterion.

    **1. src/container/scan.rs (1,268 lines post-audit) → src/container/scan/ directory**

    Per CONTEXT.md, split into:
    - `mod.rs` — re-exports for `build_container_report`, `build_source_report`, `scan_container`, `scan_source_tarball`
    - `orchestrate.rs` — main scan flow / orchestration (the top-level functions that call inventory and enrich)
    - `inventory.rs` — package detection dispatch (the code that identifies RPM, APK, dpkg, npm, pip, Go, etc.)
    - `enrich.rs` — enrichment calls (the code that calls into vuln:: to enrich findings)

    **2. src/vulndb.rs (1,447 lines post-audit) → src/vulndb/ directory**

    Per CONTEXT.md, split into:
    - `mod.rs` — re-exports, shared types (`VulnDbMeta`, table name constants)
    - `build.rs` — bulk import logic (`build_db()`, data source importers)
    - `query.rs` — cache lookup functions (`query_osv_vulns()`, `query_nvd_cve()`, etc.)
    - `fetch.rs` — download from API (`fetch_db()`, auth, progress)

    NOTE: Many query functions may have been removed in Plan 01 as dead code. Split whatever survives the audit.

    **3. src/archive.rs (1,604 lines post-audit) → src/archive/ directory**

    Read the file and identify natural split points. Likely splits by extraction format or by concern:
    - `mod.rs` — re-exports, shared types
    - Submodules by format (tar, zip, etc.) or by concern (extraction, validation, path-safety)
    - Claude determines the best split — the code structure will reveal natural boundaries

    **4. src/iso.rs (1,089 lines post-audit) → src/iso/ directory**

    Read the file and identify natural split points:
    - `mod.rs` — re-exports
    - Submodules for ISO extraction, repo metadata parsing, and package scanning
    - Preserve the existing tests (iso::tests module)

    **5. src/main.rs (953 lines)**

    Claude's discretion per CONTEXT.md. If splitting:
    - Move subcommand handler functions into `src/cli/` submodules (the cli/ directory already exists with benchmark.rs, db.rs, detect.rs, diff.rs, helpers.rs)
    - Keep only the Clap struct definitions and `main()` in main.rs

    If NOT splitting: Document the rationale (e.g., "953 lines for 12 subcommands is acceptable — each handler is 20-50 lines and co-locating them with the Clap definitions aids readability").

    **After each split:**
    1. Update the parent mod.rs re-exports
    2. Update src/main.rs imports if module paths changed
    3. `cargo build --release` — zero warnings
    4. `cargo test --locked --no-fail-fast` — all 46 tests pass
    5. ONE atomic commit per module split
  </action>
  <verify>
    <automated>cd /Users/devintripp/Desktop/GitHub/Rust/rust_scanner && cargo build --release 2>&1 | grep -c "^warning:" | xargs -I{} test {} -eq 0 && cargo test --locked --no-fail-fast 2>&1 | grep "test result:" | grep "46 passed" && find src -name "*.rs" ! -path "*/main.rs" -exec wc -l {} + | awk '$1 > 800 && $2 != "total" {exit 1}'</automated>
  </verify>
  <done>
    - src/container/scan/ exists with orchestrate.rs, inventory.rs, enrich.rs — each under 800 lines
    - src/vulndb/ exists with build.rs, query.rs, fetch.rs — each under 800 lines
    - src/archive/ exists with focused submodules — each under 800 lines
    - src/iso/ exists with focused submodules — each under 800 lines
    - src/main.rs either split or documented rationale for keeping as-is
    - All public APIs accessible at the same import paths
    - Zero compiler warnings
    - All 46 tests pass
    - One atomic commit per module split (expect ~4-5 commits)
    - Total of ~8-10 commits across both tasks in this plan
  </done>
</task>

</tasks>

<verification>
1. `find src -name "*.rs" -exec wc -l {} + | sort -rn | head -20` — no file over 800 lines (main.rs exempted if Claude documented rationale)
2. `cargo build --release 2>&1 | grep "^warning:"` — returns nothing
3. `cargo test --locked --no-fail-fast` — all 46 tests pass
4. `git log --oneline` — shows one atomic commit per module split, each with clear description
5. Public API spot check: `grep -r "pub use" src/vuln/mod.rs src/container/mod.rs` — all original re-exports still present
6. No functional changes: `git diff --stat` shows only file renames/moves, mod.rs additions, and re-export updates
</verification>

<success_criteria>
- Every source file under src/ is under 800 lines (with documented exemption for main.rs if applicable)
- Every submodule under src/vuln/ is under 800 lines with a single clearly named responsibility
- All 46 tests pass without any test code modifications
- Zero compiler warnings from `cargo build --release`
- All public function signatures remain identical — no caller outside the module needed changes
- Each module split is one atomic commit that can be independently reverted
- The diff is reviewable: code movement and re-exports only, no logic changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-audit-and-module-refactor/01-02-SUMMARY.md`
</output>
