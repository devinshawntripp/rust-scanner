---
phase: 04-scanner-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - SCAN-01

must_haves:
  truths:
    - "Git tag v1.10.0 exists in the local repository"
    - "Git tag v1.10.0 is pushed to the remote origin"
    - "GitHub Actions release workflow is triggered by the tag push"
  artifacts: []
  key_links:
    - from: "Cargo.toml"
      to: "git tag v1.10.0"
      via: "version = 1.10.0 matches tag name"
      pattern: "version.*1.10.0"
    - from: "git tag v1.10.0"
      to: ".github/workflows/release.yml"
      via: "on push tags v* triggers CI build and release"
      pattern: "tags.*v\\*"
---

<objective>
Close Gap 2 from Phase 4 verification: GitHub release v1.10.0 does not exist. Cargo.toml version is 1.10.0, binary outputs it, Cargo.lock is up to date, but no git tag or GitHub release has been created. The release.yml workflow will automatically build binaries for 4 platforms and create a GitHub release when the tag is pushed.

Purpose: Complete the v1.10.0 release cycle so that GitHub has downloadable scanner binaries matching the current codebase.

Output: Git tag v1.10.0 pushed to origin, GitHub release v1.10.0 created by CI with attached binaries.
</objective>

<execution_context>
@/Users/devintripp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devintripp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scanner-hardening/04-VERIFICATION.md

Key context:
- Cargo.toml: version = "1.10.0"
- `cargo build --locked --release` succeeds (Cargo.lock is current)
- Latest git tag: v1.9.2 (no v1.10.0 tag exists)
- `.github/workflows/release.yml` triggers on `push tags v*` — builds 4 platform binaries and creates GitHub release via softprops/action-gh-release
- Release notes are already templated in the workflow file with DMG scanning, RHEL dedup, and unit test additions
- Per CLAUDE.md release checklist: Cargo.lock must be regenerated before tagging — already done (cargo build --locked succeeds)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify release readiness and create git tag</name>
  <files></files>
  <action>
    Perform pre-release verification:

    1. Run `cargo build --locked --release` to confirm the lock file is current
    2. Run `cargo test --locked --no-fail-fast` to confirm all tests pass
    3. Verify `./target/release/scanrook --version` outputs "scanrook 1.10.0"
    4. Verify no uncommitted changes exist with `git status`
    5. Create the tag: `git tag v1.10.0`

    Do NOT push the tag yet — that requires the user's confirmation in the next task.
  </action>
  <verify>
    <automated>git tag --list v1.10.0</automated>
  </verify>
  <done>Git tag v1.10.0 exists locally. All tests pass. Binary version confirmed as 1.10.0.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push v1.10.0 tag and verify GitHub release</name>
  <files></files>
  <action>
    This is a human action checkpoint. Claude creates the tag locally (Task 1), then the user pushes it to trigger CI.

    The user should run: `git push origin v1.10.0`

    CI will build binaries for linux-amd64, linux-arm64, darwin-amd64, darwin-arm64 and create a GitHub release.
  </action>
  <verify>
    <automated>gh release view v1.10.0 --json tagName --jq .tagName 2>/dev/null || echo "NOT_RELEASED"</automated>
  </verify>
  <done>GitHub release v1.10.0 exists with 4 platform binary archives attached.</done>
</task>

</tasks>

<verification>
1. `git tag --list v1.10.0` returns "v1.10.0"
2. GitHub release v1.10.0 exists at the repo's releases page with attached binaries
3. Release notes match the template in release.yml (DMG scanning, RHEL dedup, unit tests)
</verification>

<success_criteria>
- Git tag v1.10.0 pushed to origin
- GitHub Actions release workflow completed successfully
- GitHub release v1.10.0 has 4 platform binary archives (linux-amd64, linux-arm64, darwin-amd64, darwin-arm64)
</success_criteria>

<output>
After completion, create `.planning/phases/04-scanner-hardening/04-06-SUMMARY.md`
</output>
