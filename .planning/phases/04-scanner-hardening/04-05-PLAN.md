---
phase: 04-scanner-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/archive/dmg.rs
  - src/archive/tests.rs
autonomous: true
gap_closure: true
requirements:
  - SCAN-01

must_haves:
  truths:
    - "DMG extraction documents that hdiutil (macOS) or 7z (Linux/cross-platform) is required"
    - "extract_dmg() returns a structured error when neither hdiutil nor 7z is available"
    - "build_dmg_report() falls through to binary-only scanning on extraction failure"
    - "Unit tests verify the extraction fallback chain logic"
  artifacts:
    - path: "src/archive/dmg.rs"
      provides: "DMG extraction with documented external tool requirements"
      contains: "extract_dmg"
    - path: "src/archive/tests.rs"
      provides: "Unit tests for DMG extraction fallback chain"
      contains: "test_dmg"
  key_links:
    - from: "src/archive/dmg.rs"
      to: "src/archive/detect.rs"
      via: "detect_app_packages + detect_macos_packages"
      pattern: "detect_macos_packages"
    - from: "src/archive/dmg.rs"
      to: "src/cli/detect.rs"
      via: "looks_like_dmg_input dispatches to build_dmg_report"
      pattern: "build_dmg_report"
---

<objective>
Close Gap 1 from Phase 4 verification: DMG extraction is a documented no-op stub. The actual extraction relies on external tools (hdiutil on macOS, 7z on Linux) which are already present in the worker Docker image (p7zip-full). This plan:
1. Documents the external tool requirement in the DMG module
2. Adds unit tests that verify the extraction fallback chain (native stub -> hdiutil -> 7z)
3. Adds a test for build_dmg_report's graceful fallback to binary-only scanning on extraction failure

Purpose: Satisfy SCAN-01 for DMG format — the code is functionally correct (7z IS available in production), but lacks tests proving the fallback chain works and documentation making the requirement explicit.

Output: Updated src/archive/dmg.rs with documented requirements, new DMG-specific tests in src/archive/tests.rs.
</objective>

<execution_context>
@/Users/devintripp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devintripp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scanner-hardening/04-VERIFICATION.md

Key context from verification:
- `try_extract_dmg_native()` in src/archive/dmg.rs (line 26) always bails — this is by design because dmgwiz extracts raw partition data, not a filesystem tree
- The hdiutil path (line 48-86) only runs on macOS targets via `cfg!(target_os = "macos")`
- The 7z path (line 88-102) is the cross-platform fallback and IS available in the worker Docker image (p7zip-full installed in Dockerfile)
- `build_dmg_report()` (line 106-228) already handles extraction failure gracefully — falls through to binary-only scanning
- 5 macOS package detection tests already exist in tests.rs (lines 171-302)

<interfaces>
From src/archive/dmg.rs:
```rust
fn try_extract_dmg_native(path: &str, dest: &Path) -> anyhow::Result<()>;
pub fn extract_dmg(path: &str, dest: &Path) -> anyhow::Result<()>;
pub fn build_dmg_report(path: &str, mode: ScanMode, nvd_api_key: Option<String>) -> Option<Report>;
fn scan_embedded_binaries_dmg(root: &Path, mode: &ScanMode, nvd_api_key: &Option<String>) -> Vec<Finding>;
```

From src/archive/mod.rs:
```rust
pub use dmg::build_dmg_report;
pub use detect::detect_app_packages;
pub use detect::detect_macos_packages;
```

From src/cli/detect.rs:
```rust
pub fn looks_like_dmg_input(path: &str) -> bool;
// In build_scan_report_value: if dmg_like { archive::build_dmg_report(...) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document external tool requirements and improve error messages in DMG module</name>
  <files>src/archive/dmg.rs</files>
  <action>
    Update src/archive/dmg.rs to clearly document the external tool requirements:

    1. Add a module-level doc comment at the top of the file (above the existing `//! DMG ...` line) that explicitly states:
       - DMG extraction requires either `hdiutil` (macOS) or `7z` (Linux/any OS, provided by p7zip-full)
       - The worker Docker image already includes p7zip-full
       - The Rust-native dmgwiz path is intentionally a no-op because dmgwiz extracts raw partition data, not filesystem trees
       - On extraction failure, the scanner falls through to binary-only scanning (graceful degradation)

    2. In the `extract_dmg()` function, improve the final error message to be more actionable:
       - Change "DMG extraction requires hdiutil (macOS) or 7z. Neither was found." to:
         "DMG extraction requires hdiutil (macOS) or 7z (apt: p7zip-full). Neither was found. Install p7zip-full on Linux or use macOS for DMG scanning."

    3. Make `extract_dmg` testable by making the `try_extract_dmg_native` function `pub(crate)` instead of private, so tests can verify the stub behavior directly.

    Do NOT change any functional behavior. The fallback chain (native -> hdiutil -> 7z) is correct as-is.
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -3</automated>
  </verify>
  <done>DMG module has explicit documentation of external tool requirements. Error messages are actionable. try_extract_dmg_native is pub(crate) for testability.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for DMG extraction fallback chain</name>
  <files>src/archive/tests.rs</files>
  <action>
    Add tests to src/archive/tests.rs that verify DMG extraction behavior:

    1. `test_dmg_native_extraction_always_bails` — Call `super::dmg::try_extract_dmg_native()` (now pub(crate)) with a temp directory and verify it returns an Err containing "raw disk partition data". This proves the stub behavior is intentional and documented.

    2. `test_dmg_extract_nonexistent_file_returns_error` — Call `super::dmg::extract_dmg()` with a path to a non-existent file. On the dev machine (no 7z), this should return an error about neither hdiutil nor 7z being found. On CI (with 7z), it will fail with a 7z extraction error. Both are structured errors, not panics. Assert `result.is_err()`.

    3. `test_dmg_build_report_extraction_failure_returns_some` — Create a temp file with garbage content (not a valid DMG), call `build_dmg_report()` with it. This should NOT return None — per the existing code (line 113-125), extraction failure falls through to binary-only scanning with empty packages. Assert the returned report is Some and has `scan_status == Complete` and `inventory_status == Missing` (since there are no packages and no binary findings from garbage).

    4. `test_dmg_build_report_target_type` — Same as above but also verify `report.target.target_type == "dmg"` to confirm the DMG pipeline is wired correctly.

    Add these tests after the existing macOS package detection tests section. Import `super::dmg` at the top of the test module.

    Note: These are unit tests that exercise the code paths without requiring a real DMG file or external tools. They verify the fallback chain LOGIC (native stub bails -> external tools attempted -> graceful error handling).
  </action>
  <verify>
    <automated>cd /Users/devintripp/Desktop/GitHub/scanrook/rust_scanner && cargo test --lib archive::tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>4 new DMG tests pass: native stub bails correctly, extraction errors are structured (no panics), build_dmg_report returns valid report on extraction failure, target_type is "dmg".</done>
</task>

</tasks>

<verification>
1. `cargo build --release` compiles with zero warnings
2. `cargo test --lib archive::tests` — all existing tests plus 4 new DMG tests pass
3. `cargo test` — full suite passes (no regressions)
4. src/archive/dmg.rs contains explicit documentation about p7zip-full requirement
</verification>

<success_criteria>
- DMG module documents that hdiutil or 7z (p7zip-full) is required for extraction
- try_extract_dmg_native() is tested and confirmed to always bail (intentional no-op)
- extract_dmg() returns structured errors on failure (no panics)
- build_dmg_report() returns a valid report even when extraction fails (graceful degradation verified by test)
- All tests pass with `cargo test`
</success_criteria>

<output>
After completion, create `.planning/phases/04-scanner-hardening/04-05-SUMMARY.md`
</output>
