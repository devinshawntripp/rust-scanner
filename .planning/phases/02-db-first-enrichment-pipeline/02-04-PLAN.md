---
phase: 02-db-first-enrichment-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/vuln/osv/batch.rs
  - src/vuln/osv/enrich.rs
  - src/vuln/nvd/enrich.rs
  - src/vuln/epss.rs
  - src/vuln/kev.rs
  - src/vuln/http.rs
  - src/container/scan.rs
  - src/container/cli.rs
autonomous: true
requirements:
  - SCAN-02
  - ENRICH-05
  - ENRICH-06
  - INFR-03
must_haves:
  truths:
    - "Circuit breakers are instantiated per-scan and passed to all enrichment functions"
    - "NVD 403 rate limit triggers retry with backoff; after 3 retries the CVE is skipped, not hung"
    - "After 5 consecutive failures for any API source, the circuit breaker trips and all further calls to that source are skipped for the remainder of the scan"
    - "When a circuit breaker trips, a warning is emitted via progress AND added to Report.summary.warnings"
    - "All enrichment functions use jittered TTL for PG cache freshness (not fixed 30 days)"
    - "In standalone mode (SCANROOK_CLUSTER_MODE=0), no PG or Redis connections are attempted anywhere in the enrichment pipeline"
    - "In cluster mode (SCANROOK_CLUSTER_MODE=1), no file cache reads or writes occur in the enrichment pipeline"
  artifacts:
    - path: "src/container/scan.rs"
      provides: "Circuit breaker instantiation per scan, passed to all enrichment functions, warnings collected into report"
      contains: "CircuitBreaker::new"
    - path: "src/vuln/nvd/enrich.rs"
      provides: "Circuit breaker integration — skip NVD fetch when breaker is open"
      contains: "is_open"
    - path: "src/vuln/osv/batch.rs"
      provides: "Circuit breaker integration in per-package fallback loop"
      contains: "is_open"
  key_links:
    - from: "src/container/scan.rs"
      to: "src/vuln/circuit.rs"
      via: "CircuitBreaker::new instantiation per scan"
      pattern: "CircuitBreaker::new"
    - from: "src/vuln/nvd/enrich.rs"
      to: "src/vuln/circuit.rs"
      via: "breaker.record_failure/record_success/is_open calls"
      pattern: "record_failure\\|is_open"
    - from: "src/container/scan.rs"
      to: "src/report.rs"
      via: "Collect circuit breaker warnings into report.summary.warnings"
      pattern: "summary\\.warnings"
---

<objective>
Wire circuit breakers into all enrichment functions, enforce jittered TTL everywhere, ensure strict mode separation with no cross-contamination, and collect warnings into the report summary.

Purpose: This is the safety net plan — prevents infinite hangs on API failures (SCAN-02), ensures PG cache freshness uses jitter (ENRICH-06), and verifies the complete standalone/cluster separation (INFR-03). After this plan, Phase 2 success criteria are fully met.
Output: All enrichment functions have circuit breaker support. Report includes warnings when APIs are unavailable. Mode separation is strict everywhere.
</objective>

<execution_context>
@/Users/devintripp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devintripp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-db-first-enrichment-pipeline/02-RESEARCH.md
@.planning/phases/02-db-first-enrichment-pipeline/02-01-SUMMARY.md
@.planning/phases/02-db-first-enrichment-pipeline/02-02-SUMMARY.md
@.planning/phases/02-db-first-enrichment-pipeline/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/vuln/circuit.rs:
- CircuitBreaker::new(source_name: &'static str, threshold: u32) -> Self
- CircuitBreaker::record_failure(&self)
- CircuitBreaker::record_success(&self)
- CircuitBreaker::is_open(&self) -> bool
- CircuitBreaker::source_name(&self) -> &str

From src/vuln/pg.rs:
- compute_jittered_ttl_days(base_days: i64, jitter_days: i64) -> i64

From src/report.rs:
- Summary.warnings: Vec<String> (added in Plan 01)

<!-- Current enrichment function signatures (after Plans 02-03) -->
From src/vuln/osv/batch.rs:
- pub fn osv_batch_query(packages: &Vec<PackageCoordinate>, pg: &mut Option<PgClient>) -> Value

From src/vuln/osv/enrich.rs:
- pub fn osv_enrich_findings(findings: &mut Vec<Finding>, ..., pg: &mut Option<PgClient>)

From src/vuln/nvd/enrich.rs:
- pub fn enrich_findings_with_nvd(findings: &mut Vec<Finding>, api_key: ..., pg: &mut Option<PgClient>)

From src/vuln/epss.rs:
- pub fn epss_enrich_findings(findings: &mut [Finding], pg: &mut Option<PgClient>, cache_dir: Option<&Path>)

From src/vuln/kev.rs:
- pub fn kev_enrich_findings(findings: &mut [Finding], pg: &mut Option<PgClient>, cache_dir: Option<&Path>)

From src/container/scan.rs:
- build_container_report() — the main scan pipeline orchestrator
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire circuit breakers into enrichment functions</name>
  <files>src/vuln/osv/batch.rs, src/vuln/osv/enrich.rs, src/vuln/nvd/enrich.rs, src/vuln/epss.rs, src/vuln/kev.rs</files>
  <action>
Add a `&CircuitBreaker` parameter to each enrichment function and integrate it:

1. **osv_batch_query** — Add `breaker: &CircuitBreaker` parameter. At the top, check `if breaker.is_open() { return empty results; }`. In the per-chunk HTTP request section: on success call `breaker.record_success()`, on failure call `breaker.record_failure()` and if `breaker.is_open()` break out of the chunk loop. In the per-package fallback loop (~line 204): check `if breaker.is_open() { break; }` before each attempt.

2. **osv_enrich_findings** — Add `breaker: &CircuitBreaker` parameter. At the top, check `if breaker.is_open() { return; }`. Before each HTTP call to fetch individual OSV vulns: check `breaker.is_open()`. On HTTP failure: `breaker.record_failure()`. On success: `breaker.record_success()`.

3. **enrich_findings_with_nvd** — Add `breaker: &CircuitBreaker` parameter. At the top, check `if breaker.is_open() { return; }`. Around the `nvd_get_json()` calls: on `None` return call `breaker.record_failure()` and check `if breaker.is_open() { break; }`. On success: `breaker.record_success()`.

4. **epss_enrich_findings** — Add `breaker: &CircuitBreaker` parameter. At the top, check `if breaker.is_open() { return; }`. Around HTTP call: record_failure/success, check is_open.

5. **kev_enrich_findings** — Add `breaker: &CircuitBreaker` parameter. At the top, check `if breaker.is_open() { return; }`. Around HTTP call: record_failure/success, check is_open.

NOTE: OSV and NVD should share separate circuit breakers (different source names). Create one per API source: "osv", "nvd", "epss", "kev". All 4 passed from the scan pipeline function.
  </action>
  <verify>
    <automated>cargo check 2>&1 | head -30</automated>
  </verify>
  <done>All 5 enrichment functions accept and use circuit breakers. Early-exit when breaker is open. Failure/success recording at every HTTP call site.</done>
</task>

<task type="auto">
  <name>Task 2: Wire circuit breakers in scan pipelines, enforce mode separation, collect warnings</name>
  <files>src/container/scan.rs, src/container/cli.rs, src/vuln/osv/enrich.rs, src/vuln/nvd/enrich.rs</files>
  <action>
1. In `build_container_report()` in `src/container/scan.rs`, create circuit breakers at the top:
   ```rust
   let osv_breaker = crate::vuln::CircuitBreaker::new("osv", 5);
   let nvd_breaker = crate::vuln::CircuitBreaker::new("nvd", 5);
   let epss_breaker = crate::vuln::CircuitBreaker::new("epss", 5);
   let kev_breaker = crate::vuln::CircuitBreaker::new("kev", 5);
   ```
   Pass the appropriate breaker to each enrichment call.

2. Do the same in `scan_container()` in `src/container/cli.rs` and in every other scan pipeline entry point that was updated in Plans 02-03 (binary.rs, sbom.rs, iso/report.rs, archive/scan.rs, archive/dmg.rs). Each pipeline gets its own set of breakers.

3. After all enrichment calls complete, collect warnings from tripped breakers into the report:
   ```rust
   let breakers = [&osv_breaker, &nvd_breaker, &epss_breaker, &kev_breaker];
   for b in &breakers {
       if b.is_open() {
           report.summary.warnings.push(format!(
               "{} unavailable — results may be incomplete ({} consecutive failures)",
               b.source_name(), /* threshold */5
           ));
       }
   }
   ```

4. Audit mode separation — verify in EACH enrichment function:
   - Cluster mode (SCANROOK_CLUSTER_MODE=1): ONLY PG path taken, no file cache read/write
   - Standalone mode (SCANROOK_CLUSTER_MODE=0): ONLY file cache path taken, no PG calls
   - Check `osv/enrich.rs` and `nvd/enrich.rs` which existed before Phase 2 — ensure they also use jittered TTL (replace any hardcoded TTL constants with `compute_jittered_ttl_days(30, 7)` calls).

5. In `src/vuln/osv/enrich.rs` and `src/vuln/nvd/enrich.rs`, find any existing `compute_dynamic_ttl_days` calls and replace with `compute_jittered_ttl_days(30, 7)` to add the ±7 day jitter per the locked decision.

6. Verify the HTTP timeout values are set on all client builders (they already are per the research — just confirm no new unguarded HTTP calls were introduced).
  </action>
  <verify>
    <automated>cargo test --locked --no-fail-fast 2>&1 | tail -20</automated>
  </verify>
  <done>Circuit breakers wired into all scan pipelines. Warnings collected into report.summary.warnings. Jittered TTL used everywhere. Mode separation verified — no cross-contamination between standalone and cluster paths. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo build --release` compiles cleanly
- `cargo test --locked --no-fail-fast` all tests pass (including circuit breaker tests from Plan 01)
- `grep -rn "CircuitBreaker::new" src/` — shows breaker instantiation in every scan pipeline
- `grep -rn "is_open" src/vuln/` — shows early-exit checks in all enrichment functions
- `grep -rn "compute_jittered_ttl_days" src/vuln/` — shows jitter used in osv/enrich, nvd/enrich, osv/batch, epss, kev
- `grep -rn "summary.warnings" src/` — shows warning collection in scan pipelines
- Phase 2 success criteria verification:
  1. With warm PG cache: zero OSV/NVD HTTP requests (verified by observing pg_cache progress events)
  2. With cold cache: API calls fire and results written to PG (verified by pg_put progress events)
  3. PG entries use jittered TTL (confirmed by compute_jittered_ttl_days usage)
  4. SCANROOK_CLUSTER_MODE=0 never touches PG; =1 skips file cache (code audit)
  5. NVD 403 triggers retry with backoff, circuit breaker trips after 5 failures (code audit + unit tests)
</verification>

<success_criteria>
- All enrichment functions have circuit breaker integration
- Circuit breakers are scan-scoped (instantiated per scan, not static)
- Report summary includes warnings when APIs are unavailable
- Jittered TTL (30 ± 7 days) used for ALL PG cache freshness checks
- Strict mode separation: standalone = file cache only, cluster = PG only
- All HTTP API calls have timeouts (confirmed, not newly introduced)
- `cargo test` passes all tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-db-first-enrichment-pipeline/02-04-SUMMARY.md`
</output>
